# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# --- Node / Vite ---
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- Env & secrets ---
.env
.env.*
!.env.example
*.token.json
credentials.json
token.json
client_secret*.json

# --- Local data / cache ---
backend/data/*.db
backend/data/*.sqlite3
*.sqlite3
data/cache/
# vault is its own git repo (Hermes/GitHub owns it) — don't nest in the main repo
backend/data/vault/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Claude memory (keep out of repo history) ---
.claude/
